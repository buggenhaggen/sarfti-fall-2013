//Функция pop() удаляет из массива его последний элемент и возвращает его в качестве результата.
#include "conio.h"
#include "iostream"
#include "stack"
 
using namespace std;
int main()
{
    setlocale(LC_ALL,"Russian");
 
    stack<int> original;//первый стек 
    stack<int> auxiliary;//второй вспомогательный стек
    
    int i;//первая переменная
    int m;//вторая переменная
	original.pop();
        
    getch();
}